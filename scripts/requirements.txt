# PM Toolkit Python Dependencies
# Install with: pip install -r requirements.txt

# Data analysis
pandas>=3.0.5
numpy>=2.5.2

# Statistics
# Optional. Only 4 scripts still need scipy, for distributions the stdlib
# lacks: cohort-comparison-tool and velocity-trend-analyzer (Student's t),
# data-drift-detector (Kolmogorov-Smirnov), adoption-funnel-analyzer.
# All four degrade gracefully without it. Everything that needed only the
# inverse normal CDF now uses statistics.NormalDist from the stdlib.
scipy>=1.18.0
statsmodels>=0.14.6

# Visualization
matplotlib>=3.11.1
seaborn>=0.13.2

# NLP / Sentiment Analysis
textblob>=0.20.1
nltk>=3.10.3

# HTTP requests (for API integrations)
requests>=2.34.2

# Jupyter notebooks
jupyter>=1.1.1
ipykernel>=7.3.0

# Utilities
python-dateutil>=2.9.0.post0
tqdm>=4.70.0

# LLM APIs (for evaluation scripts)
openai>=3.3.1
anthropic>=0.124.0

# Eval metrics (BLEU, ROUGE)
rouge-score>=0.1.2

# Token counting (optional; for scripts/token-counter.py accurate counts)
# tiktoken>=0.5.0

